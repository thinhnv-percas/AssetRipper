"""Port of Source/AssetRipper.IO.Files/BundleFiles/RawWeb.

The legacy pre-Unity5 "UnityRaw"/"UnityWeb" bundle formats (`.raw`/`raw/`, `.web`/`web/`
subpackages here) -- still just barely relevant because Unity WebGL builds up through
5.x continued to accept `.unity3d` files in this shape for backwards compatibility, and
some archived WebPlayer-era games use it as their only bundle format.
"""
